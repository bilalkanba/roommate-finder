# === FastAPI & ASGI server ===
fastapi==0.109.0
uvicorn[standard]==0.27.0

# === Data validation & settings ===
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6   # for form data (file uploads)

# === Supabase client (PostgreSQL + Auth) ===
supabase==2.3.4

# === OpenAI for match explanations ===
openai==1.10.0

# === JWT token verification (Supabase issues JWTs) ===
pyjwt==2.8.0
python-jose[cryptography]==3.3.0

# === HTTP client (for internal calls) ===
httpx==0.25.2

# === ORM & PostgreSQL driver ===
sqlalchemy==2.0.25
psycopg2-binary==2.9.9

# === Dev & testing ===
pytest==8.3.4
pytest-asyncio==0.24.0
black==24.1.1
ruff==0.1.14
